const lyrics = ["Branching is used in version control" ,
" software management to maintain stability" ,
"while isolated changes are made to code."];
const newLine = lyrics.join(":");
console.log(newLine);